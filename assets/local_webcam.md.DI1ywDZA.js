import{_ as s,o as a,c as i,a4 as t}from"./chunks/framework.CmuCl-jw.js";const k=JSON.parse('{"title":"Live-Image Proxy","description":"","frontmatter":{},"headers":[],"relativePath":"local/webcam.md","filePath":"local/webcam.md"}'),n={name:"local/webcam.md"};function h(r,e,o,l,d,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="live-image-proxy" tabindex="-1">Live-Image Proxy <a class="header-anchor" href="#live-image-proxy" aria-label="Permalink to “Live-Image Proxy”">​</a></h1><p>The Arcsecond CLI ships a native live-image proxy that lets <a href="/local/">Arcsecond.local</a> — the self-hosted version of Arcsecond — access <strong>USB webcams</strong>, <strong>all-sky cameras</strong> and <strong>network cameras</strong>.</p><p>Docker Desktop on Windows and macOS does not forward USB devices into containers, and may not have access to host filesystem paths where all-sky software writes its images. The proxy solves both: it runs natively on the host, where it has direct USB access and can read local files, and exposes the feeds over HTTP and WebSocket. The Arcsecond backend container reaches the proxy via <code>host.docker.internal</code>.</p><p>A camera with an address of its own goes through the same proxy without being attached to the host at all: the proxy connects out to it over your network. That covers network cameras, and all-sky cameras whose software runs on another machine and publishes its image over HTTP. One proxy serves every camera it can reach.</p><h2 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to “Installation”">​</a></h2><p>The proxy requires extra dependencies (OpenCV and aiohttp) that are not installed by default. Install them with the <code>webcam</code> extra:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> arcsecond[webcam]</span></span></code></pre></div><p>The extra is named <code>webcam</code> for backward compatibility, but it covers all-sky support too.</p><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>If your shell treats square brackets specially (e.g. zsh), quote the package name:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;arcsecond[webcam]&#39;</span></span></code></pre></div></div><div class="warning custom-block"><p class="custom-block-title">Windows users — use PowerShell, not WSL</p><p>On Windows, the Arcsecond CLI <strong>must be installed and run natively using PowerShell</strong> (or the standard Windows Command Prompt). Do <strong>not</strong> use the Bash shell that comes with WSL (Windows Subsystem for Linux).</p><p>WSL runs in a virtualised Linux environment that does not have direct access to the host&#39;s USB devices. As a result, the webcam proxy cannot see cameras attached to the Windows machine when launched from inside WSL.</p></div><h2 id="how-the-commands-fit-together" tabindex="-1">How the commands fit together <a class="header-anchor" href="#how-the-commands-fit-together" aria-label="Permalink to “How the commands fit together”">​</a></h2><p>Registering a camera and serving it are two separate jobs, and two separate commands. <code>add</code> never starts a proxy, and <code>proxy start</code> never registers a camera:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                          # register: a USB webcam</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /srv/allsky/latest.jpg</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # register: an all-sky camera</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                           # serve everything registered</span></span></code></pre></div><p>There is one proxy, and it serves every kind of camera. Three things are worth knowing before you start.</p><p><strong>Cameras are remembered.</strong> They come back on their own the next time the proxy starts, so a machine that reboots overnight comes back with its cameras. You do not need to re-type anything.</p><p><strong>Order does not matter.</strong> A camera added while the proxy is running is handed to it straight away, so neither the proxy nor Arcsecond.local needs restarting.</p><p><strong>Nothing is lost by running a command twice.</strong> Adding a camera that is already registered simply hands back the id it already has.</p><h3 id="ids" tabindex="-1">Ids <a class="header-anchor" href="#ids" aria-label="Permalink to “Ids”">​</a></h3><p>Every camera gets a three-character id — <code>k3f</code>, <code>r4t</code> — the moment it is registered, and that id is the only handle you ever type afterwards. It is derived from what identifies the camera, so registering the same one twice gives the same id rather than a second entry, and it is not positional: unplugging the first of three webcams does not renumber the other two.</p><p>The alphabet leaves out characters that get misread off a screen, so there is no <code>0</code>/<code>O</code> or <code>1</code>/<code>l</code>/<code>I</code> to guess at.</p><h2 id="webcams" tabindex="-1">Webcams <a class="header-anchor" href="#webcams" aria-label="Permalink to “Webcams”">​</a></h2><p><code>arcsecond webcam</code> covers every camera that is not an all-sky camera, whether it is plugged in here over USB or reached over the network. Same list, same <code>add</code>, same <code>forget</code> — the address is simply what you pass to <code>add</code> instead of a device index.</p><h3 id="seeing-what-is-registered" tabindex="-1">Seeing what is registered <a class="header-anchor" href="#seeing-what-is-registered" aria-label="Permalink to “Seeing what is registered”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span></span></code></pre></div><p>Prints what is registered and probes nothing, so it answers instantly. USB cameras show the resolution and frame rate recorded when they were added.</p><h3 id="seeing-what-is-there" tabindex="-1">Seeing what is there <a class="header-anchor" href="#seeing-what-is-there" aria-label="Permalink to “Seeing what is there”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> detect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [--timeout </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">2.0]</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [--no-network]</span></span></code></pre></div><p>Looks at the hardware and says how it lines up with what is registered: what is newly detected, what is registered and present, what is registered and missing. It registers nothing.</p><p>Network cameras cannot be discovered — there is no way to ask a network which of it is a camera — so a registered one is confirmed by connecting to its address instead. <code>--no-network</code> skips that and lists them without checking.</p><h3 id="adding-one" tabindex="-1">Adding one <a class="header-anchor" href="#adding-one" aria-label="Permalink to “Adding one”">​</a></h3><p>A webcam plugged into this machine is registered by the device index <code>detect</code> printed:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --label</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Guide cam&quot;</span></span></code></pre></div><p>The device is opened once while registering, both to tell you whether anything is there and to record its resolution and frame rate, so that listing it later never has to open it again. A camera registered while unplugged is registered anyway — re-run the same command once it is plugged in to record its specs.</p><p>A camera on the network is registered by its address instead:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rtsp://192.168.1.42:554/stream1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --label</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Dome&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://192.168.1.50/snapshot.jpg</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Address starts with</th><th>Typical camera</th></tr></thead><tbody><tr><td><code>rtsp://</code>, <code>rtsps://</code></td><td>IP and security cameras, video stream</td></tr><tr><td><code>http://</code>, <code>https://</code></td><td>Cameras serving a still image or MJPEG</td></tr></tbody></table><p>Your camera&#39;s manual gives the address to use.</p><h3 id="testing-one" tabindex="-1">Testing one <a class="header-anchor" href="#testing-one" aria-label="Permalink to “Testing one”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rtsp://192.168.1.42:554/stream1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> k3f</span></span></code></pre></div><p>Pulls one image and reports what came back. It takes either an address you have not registered yet, so you can check it before adding it, or the id of a registered camera, so you can check that one without retyping its address.</p><h3 id="removing-one" tabindex="-1">Removing one <a class="header-anchor" href="#removing-one" aria-label="Permalink to “Removing one”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> forget</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> k3f</span></span></code></pre></div><p>Works for every kind of webcam, whether plugged in here or on the network. If the proxy is running, it stops serving the camera straight away.</p><h2 id="all-sky-cameras" tabindex="-1">All-sky cameras <a class="header-anchor" href="#all-sky-cameras" aria-label="Permalink to “All-sky cameras”">​</a></h2><p>An all-sky camera is registered by the JPEG its software keeps up to date. That is either a <strong>path</strong>, when the software runs on this machine, or an <strong>address</strong>, when it runs on another machine and publishes the image over HTTP. The two most common stacks are auto-discovered when they write here:</p><ul><li><strong>Thomas Jacquin&#39;s allsky</strong> (<a href="https://github.com/AllskyTeam/allsky" target="_blank" rel="noreferrer">github.com/AllskyTeam/allsky</a>), typically on a Raspberry Pi</li><li><strong>indi-allsky</strong> (<a href="https://github.com/aaronwmorris/indi-allsky" target="_blank" rel="noreferrer">github.com/aaronwmorris/indi-allsky</a>), INDI-based</li></ul><h3 id="seeing-what-is-registered-1" tabindex="-1">Seeing what is registered <a class="header-anchor" href="#seeing-what-is-registered-1" aria-label="Permalink to “Seeing what is registered”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span></span></code></pre></div><p>Prints the cameras you have registered, and probes nothing — so it answers instantly, and gives the same answer every time. Each camera has a three-character id, such as <code>r4t</code>. That id is the only handle you ever need, and it does not change.</p><h3 id="seeing-what-is-there-1" tabindex="-1">Seeing what is there <a class="header-anchor" href="#seeing-what-is-there-1" aria-label="Permalink to “Seeing what is there”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> detect</span></span></code></pre></div><p>Looks at the well-known locations below and says how they line up with what is registered: what is new, what is registered and present, what is registered and missing. It registers nothing.</p><table tabindex="0"><thead><tr><th>Path</th><th>Software</th></tr></thead><tbody><tr><td><code>~/allsky/tmp/image.jpg</code></td><td>Jacquin allsky</td></tr><tr><td><code>~/allsky/html/allsky/image.jpg</code></td><td>Jacquin allsky, local website</td></tr><tr><td><code>/var/www/html/allsky/image.jpg</code></td><td>Jacquin allsky, installs before 2023</td></tr><tr><td><code>/var/lib/indi-allsky/images/latest.jpg</code></td><td>indi-allsky</td></tr><tr><td><code>~/indi-allsky/latest.jpg</code></td><td>indi-allsky</td></tr></tbody></table><p>A camera you registered yourself is checked at its own path as well, so writing to an unusual location never gets it reported as missing. One registered by address has no path to look at, so it is confirmed by connecting to it, the way a network camera is.</p><h3 id="adding-one-by-path" tabindex="-1">Adding one by path <a class="header-anchor" href="#adding-one-by-path" aria-label="Permalink to “Adding one by path”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /srv/allsky/latest.jpg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --label</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Roof&quot;</span></span></code></pre></div><p>The path may be a fixed file, a symlink, or a pattern — with a pattern, the newest matching file wins:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;/var/data/allsky/*.jpg&#39;</span></span></code></pre></div><p>The camera is registered whether or not the file is there yet. All-sky software often writes its first image only at dusk, and refusing until then would just mean coming back to retype the command — the same is true of an address that does not answer, which is registered with a note saying so rather than refused.</p><h3 id="adding-one-by-address" tabindex="-1">Adding one by address <a class="header-anchor" href="#adding-one-by-address" aria-label="Permalink to “Adding one by address”">​</a></h3><p>When the all-sky software runs on another machine — a Raspberry Pi in the garden, say — you do not need a second proxy over there. Register the address of the image it publishes, and the proxy fetches it over your network:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://allsky.local/current/tmp/image.jpg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --label</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Roof&quot;</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Finding the address</p><p>Open your all-sky software&#39;s web interface, right-click the live image and copy its address, dropping any <code>?_ts=...</code> at the end. For Thomas Jacquin&#39;s allsky it is usually <code>http://&lt;host&gt;/current/tmp/image.jpg</code> — a web-server alias for the image the capture loop rewrites every cycle, which is why there is no such folder on the Pi itself.</p><p>Check it before registering, from the machine that will run the proxy:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -sSI</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://allsky.local/current/tmp/image.jpg</span></span></code></pre></div><p>You want <code>200</code> and <code>Content-Type: image/jpeg</code>. <code>curl</code> uses your machine&#39;s own resolver, so a <code>.local</code> name that fails here may still work — see below.</p></div><h3 id="on-local-names" tabindex="-1">On <code>.local</code> names <a class="header-anchor" href="#on-local-names" aria-label="Permalink to “On .local names”">​</a></h3><p>All-sky software on a Raspberry Pi is usually reached at a name like <code>skykot.local</code>, and that name works differently from every other. It is in no DNS server: the Pi answers for itself over <strong>mDNS</strong> — multicast DNS, the protocol behind Bonjour and Avahi — and the machine asking has to know how to listen. Most do. Some — Windows machines in particular, on some network profiles — send the question to the configured DNS server instead, which knows nothing about <code>.local</code> and answers with a failure:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Cannot connect to host skykot.local:80 [DNS server returned general failure]</span></span></code></pre></div><p>The proxy handles this itself since 3.19.0: when your machine&#39;s resolver has no answer for a <code>.local</code> name, it sends its own mDNS query, exactly as the browser you copied the address from does. Nothing to configure, and the name — not an address — is what stays registered, so the camera survives its next DHCP lease.</p><p>Two things it cannot do. An <code>rtsp://</code> camera is opened by FFmpeg, which resolves names its own way, so a video stream at a <code>.local</code> name still depends on your machine. And if what blocks mDNS is a firewall rather than a missing resolver, the proxy&#39;s query is blocked along with everyone else&#39;s — mDNS is UDP to <code>224.0.0.251:5353</code>, which is also the rule to look for if you would rather allow it than work around it. <code>add</code> says when nothing answers, and the Pi&#39;s IP address is then the reliable form:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://192.168.1.42/current/tmp/image.jpg</span></span></code></pre></div><p>Give the Pi a DHCP reservation in your router if you register it by address, so tomorrow&#39;s lease does not point the proxy at somebody&#39;s laptop.</p><p>It remains an all-sky camera in every way that matters: it is listed by <code>arcsecond allsky</code>, reported to Arcsecond.local as <code>allsky</code>, and polled at the cadence of an all-sky camera rather than a webcam&#39;s. Only the transport differs, and the proxy asks the server whether the picture has changed rather than downloading it again, so the traffic between two real images is negligible.</p><div class="warning custom-block"><p class="custom-block-title">A video stream is not an all-sky camera</p><p>An <code>rtsp://</code> address is a camera sending video, which is a webcam — register it with <code>arcsecond webcam add</code>. <code>arcsecond allsky add</code> takes the JPEG your all-sky software publishes.</p></div><h3 id="if-the-image-needs-a-password" tabindex="-1">If the image needs a password <a class="header-anchor" href="#if-the-image-needs-a-password" aria-label="Permalink to “If the image needs a password”">​</a></h3><p>Most all-sky software serves its image without one, even when its web interface asks you to log in. If yours does require a password, write it as <code>\${VARIABLE}</code> rather than in the clear, exactly as for a network camera:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> SKY_PASSWORD</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mypassword&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://allsky:\${SKY_PASSWORD}@10.0.0.9/current/tmp/image.jpg&#39;</span></span></code></pre></div><p>The same rules apply, including where the variable needs to be set — see <a href="#cameras-that-need-a-password">Cameras that need a password</a>.</p><h3 id="removing-one-1" tabindex="-1">Removing one <a class="header-anchor" href="#removing-one-1" aria-label="Permalink to “Removing one”">​</a></h3><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> allsky</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> forget</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> r4t</span></span></code></pre></div><p>Give the id printed by <code>arcsecond allsky</code>. The camera stops being remembered, and a running proxy stops serving it straight away.</p><h2 id="cameras-that-need-a-password" tabindex="-1">Cameras that need a password <a class="header-anchor" href="#cameras-that-need-a-password" aria-label="Permalink to “Cameras that need a password”">​</a></h2><p>Many network cameras require a username and password, written into the address before the host:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>rtsp://admin:mypassword@192.168.1.42:554/stream1</span></span></code></pre></div><p>Typing the password directly would leave it in your shell history, and would make it visible to anyone listing running processes on the machine. Write it as <code>\${VARIABLE}</code> instead, and put the password in that variable:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DOME_CAM_PASSWORD</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mypassword&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webcam</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rtsp://admin:\${DOME_CAM_PASSWORD}@192.168.1.42:554/stream1&#39;</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Use single quotes</p><p>Single quotes around the address stop your shell from substituting the variable itself. The CLI does the substitution, and reports a clear error if the variable is not set — before anything is registered.</p></div><p>Passwords are never shown again afterwards. They are replaced by <code>***</code> in the proxy&#39;s output, in its logs, and in anything it reports to Arcsecond.local. The remembered camera keeps the <code>\${DOME_CAM_PASSWORD}</code>, so the password is never written to disk.</p><div class="warning custom-block"><p class="custom-block-title">Set the variable where the proxy starts</p><p>Because only the variable is remembered, the proxy needs to be able to read it again the next time it starts. If you set it by hand in one terminal, it will be gone after a reboot, and that camera will be skipped with a message naming the variable while the others carry on. Set it where the proxy actually starts — in your shell profile, or in the service that launches it — and it will always be there.</p></div><h2 id="running-the-proxy" tabindex="-1">Running the proxy <a class="header-anchor" href="#running-the-proxy" aria-label="Permalink to “Running the proxy”">​</a></h2><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span></span></code></pre></div><p>Serves every registered camera, of every kind, over one proxy. It starts in the background and gives you your prompt back; add <code>--foreground</code> to run it in the terminal and watch it instead, where <code>Ctrl-C</code> stops it.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [--port </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">8765]</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [--host </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0.0.0.0]</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [--log-level </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">info]</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [--foreground]</span></span></code></pre></div><p>Two commands go with it:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span></code></pre></div><p>Says whether the proxy is running and what it is serving:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>The proxy is running on port 8765  (http://127.0.0.1:8765/detect)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Serving 2 camera(s):</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  ID   TRANSPORT  CAMERA                                     NAME</span></span>
<span class="line"><span>  5ak  usb        device index 0                             Guide cam</span></span>
<span class="line"><span>  m27  http       http://allsky.local/current/tmp/image.jpg  Roof</span></span></code></pre></div><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [--timeout </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">10.0]</span></span></code></pre></div><p>Works from any terminal: the proxy records where it is when it starts, so there is nothing to remember and no port to type. Your registered cameras are untouched — this stops serving them, and <code>forget</code> is what drops one.</p><h3 id="where-its-files-are" tabindex="-1">Where its files are <a class="header-anchor" href="#where-its-files-are" aria-label="Permalink to “Where its files are”">​</a></h3><table tabindex="0"><thead><tr><th>File</th><th>What it is</th></tr></thead><tbody><tr><td><code>~/.config/arcsecond/live-image-sources.json</code></td><td>the cameras you registered</td></tr><tr><td><code>~/.config/arcsecond/live-image-proxy.json</code></td><td>where the running proxy is</td></tr><tr><td><code>~/.config/arcsecond/live-image-proxy.log</code></td><td>what a background proxy printed</td></tr></tbody></table><p>You should not need to touch any of them — <code>add</code> and <code>forget</code> write the first, and the proxy writes the other two — but they are plain text, so you can always look. <code>ARCSECOND_CONFIG_DIR</code> moves the whole directory elsewhere, and a proxy started by the CLI inherits it.</p><h2 id="connecting-arcsecond-local" tabindex="-1">Connecting Arcsecond.local <a class="header-anchor" href="#connecting-arcsecond-local" aria-label="Permalink to “Connecting Arcsecond.local”">​</a></h2><p>No extra configuration is needed when the proxy runs on the same machine as Arcsecond.local. The backend defaults to <code>http://host.docker.internal:8765</code>, so it can reach the proxy out of the box.</p><p>The typical workflow is:</p><ol><li>Register your cameras: <code>arcsecond webcam add ...</code>, <code>arcsecond allsky add ...</code></li><li>Start the proxy on the host: <code>arcsecond proxy start</code></li><li>Start Arcsecond.local: <code>docker compose up -d</code></li></ol><p>Cameras added later, with the proxy already running, appear without either the proxy or Arcsecond.local being restarted.</p><h3 id="remote-proxy" tabindex="-1">Remote proxy <a class="header-anchor" href="#remote-proxy" aria-label="Permalink to “Remote proxy”">​</a></h3><div class="tip custom-block"><p class="custom-block-title">A camera with an address does not need this</p><p>A camera reached by URL — a network camera, or an all-sky camera publishing its image over HTTP — is contacted by the proxy over the network, wherever that proxy runs. Register it on the proxy you already have; there is no need for a second one. This section is only about cameras <strong>physically plugged into</strong>, or writing to the disk of, another computer.</p></div><p>If cameras are attached to a different machine on the same network (e.g. a dome PC), start the proxy there and set <code>LIVE_IMAGE_PROXY_URL</code> in the <code>.env</code> file used by <code>docker-compose.yml</code>:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>LIVE_IMAGE_PROXY_URL=http://192.168.1.42:8765</span></span></code></pre></div><p>Replace <code>192.168.1.42</code> with the actual IP address or hostname. When this variable is not set, the backend falls back to <code>http://host.docker.internal:8765</code> (the Docker way of saying <code>http://localhost:8765</code> from within a container).</p><div class="warning custom-block"><p class="custom-block-title">Changing this needs the container recreated</p><p>A container keeps the environment it was created with, so editing <code>.env</code> alone changes nothing for a backend that is already running, and <code>docker compose restart</code> reuses that same environment. Recreate it instead:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --force-recreate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> backend</span></span></code></pre></div><p>If the value has not changed, there is nothing to do. <code>docker exec arcsecond-api printenv LIVE_IMAGE_PROXY_URL</code> says what the running container actually has.</p></div><p>The previous variable name <code>WEBCAM_PROXY_URL</code> is still accepted as a deprecated fallback.</p><h2 id="endpoints" tabindex="-1">Endpoints <a class="header-anchor" href="#endpoints" aria-label="Permalink to “Endpoints”">​</a></h2><table tabindex="0"><thead><tr><th>Endpoint</th><th>Method</th><th>Description</th></tr></thead><tbody><tr><td><code>/health</code></td><td>GET</td><td>Liveness check (<code>{&quot;status&quot;: &quot;ok&quot;}</code>)</td></tr><tr><td><code>/detect</code></td><td>GET</td><td>JSON list of available sources</td></tr><tr><td><code>/stream/{id}</code></td><td>WebSocket</td><td>Continuous JPEG frame stream</td></tr><tr><td><code>/sources</code></td><td>POST</td><td>Add cameras to the running proxy</td></tr><tr><td><code>/sources/{id}</code></td><td>DELETE</td><td>Remove one</td></tr></tbody></table><p><code>{id}</code> is the camera&#39;s three-character id, exactly as <code>arcsecond webcam</code> and <code>arcsecond allsky</code> print it. There is no second, prefixed form of it.</p><p><code>/detect</code> lists what is registered without opening or contacting anything, so one camera being switched off never holds the answer up for the others. Each source reports a <code>kind</code> of <code>webcam</code> or <code>allsky</code> — what the camera <em>is</em> — and an <code>extra.transport</code> of <code>usb</code>, <code>rtsp</code>, <code>http</code> or <code>file</code>, which is how it is reached:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;m27&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;allsky&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;label&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Roof&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;extra&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://allsky.local/current/tmp/image.jpg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;transport&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The last two endpoints are how <code>add</code> and <code>forget</code> reach a proxy that is already running. They answer only to commands run <strong>on the same machine as the proxy</strong>, so no one else on your network can change which cameras it serves. Reading — detection and streaming — is unaffected.</p><h2 id="stream-protocol" tabindex="-1">Stream protocol <a class="header-anchor" href="#stream-protocol" aria-label="Permalink to “Stream protocol”">​</a></h2><p>Each frame is sent as a JSON message over the WebSocket connection:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;frame&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;format&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;jpeg/base64&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;base64-encoded JPEG&gt;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>How often one arrives depends on the camera:</p><table tabindex="0"><thead><tr><th>Camera</th><th>Cadence</th></tr></thead><tbody><tr><td>USB webcam, RTSP camera</td><td>~10 fps</td></tr><tr><td>HTTP still-image camera</td><td>checked every second, sent only when changed</td></tr><tr><td>All-sky camera, file or HTTP</td><td>checked every five seconds, sent only when changed</td></tr></tbody></table><p>An all-sky image typically changes every 30–120 seconds, depending on the camera&#39;s exposure cadence. Nothing is sent in between: a file source watches the file&#39;s modification time, and an HTTP source asks the server whether the picture has changed rather than downloading it again.</p><p>If the source becomes unavailable (device unplugged, file removed, camera off) the proxy sends an error message before closing the connection:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Cannot open webcam at device index 0.&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div>`,126)])])}const g=s(n,[["render",h]]);export{k as __pageData,g as default};
