import{_ as o,o as t,c as a,a4 as n}from"./chunks/framework.DA9C4Pbl.js";const u=JSON.parse('{"title":"Troubleshooting","description":"","frontmatter":{"title":"Troubleshooting","visibility":"public","audience":"operator","tier":"guide","source":"handwritten"},"headers":[],"relativePath":"guides/operating/troubleshooting.md","filePath":"guides/operating/troubleshooting.md"}'),s={name:"guides/operating/troubleshooting.md"};function i(r,e,d,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to “Troubleshooting”">​</a></h1><h2 id="windows-docker-desktop-data-is-not-writable-troubleshooting-blurb-copy-paste" tabindex="-1">Windows + Docker Desktop: “/data is not writable” troubleshooting blurb (copy/paste) <a class="header-anchor" href="#windows-docker-desktop-data-is-not-writable-troubleshooting-blurb-copy-paste" aria-label="Permalink to “Windows + Docker Desktop: “/data is not writable” troubleshooting blurb (copy/paste)”">​</a></h2><ul><li>Check Docker Desktop is running (the whale icon should be active). If it isn’t, <code>arcsecond start</code> waits a few seconds for Docker and then says so, rather than hanging.</li><li>Verify the host folder exists and is accessible: create the directory referenced by SHARED_DATA_PATH in File Explorer (and ensure you can create a file in it).</li><li>Avoid network / cloud-synced paths at first (common culprits: UNC paths like \\server\\share, mapped drives, OneDrive/SharePoint-synced folders). Prefer something simple like C:\\Users&lt;name&gt;\\ArcsecondData.</li><li>If you use another drive (D:\\ / external drive): enable sharing in Docker Desktop. Go to Docker Desktop → Settings → Resources → File sharing (wording may vary) and add the drive/folder, then Apply &amp; Restart.</li><li>Confirm the mount from inside a container (quick sanity check): Run a one-off container that writes a test file to /data and confirm the file appears on the host. If it can’t write, it’s almost always a sharing/permissions/path issue.</li></ul><h2 id="ports-are-not-available-exposing-port-tcp-127-0-0-1-5432" tabindex="-1">&quot;ports are not available: exposing port TCP 127.0.0.1:5432&quot; <a class="header-anchor" href="#ports-are-not-available-exposing-port-tcp-127-0-0-1-5432" aria-label="Permalink to “&quot;ports are not available: exposing port TCP 127.0.0.1:5432&quot;”">​</a></h2><p>The full line, shown by <code>arcsecond start</code> after <code>docker compose up</code> failed, is <code>Error response from daemon: ports are not available: exposing port TCP 127.0.0.1:5432 -&gt; 127.0.0.1:0: listen tcp4 127.0.0.1:5432: bind: Only one usage of each socket address (protocol/network address/port) is normally permitted</code> on Windows, or ends with <code>bind: address already in use</code> on macOS and Linux. The <code>arcsecond-db</code> container does not start, and neither does anything that depends on it.</p><p>Another program on the machine already listens on port 5432 — usually a PostgreSQL server installed directly on the machine, sometimes a container from another project. The <code>docker-compose.yml</code> written by CLI versions up to 3.19 asked Docker to publish the database on that same port of the machine (and Redis on 6379), so the two could not coexist. Nothing in Arcsecond ever used that port: every container reaches the database over Docker&#39;s internal network, and every CLI command that needs it (<a href="/guides/operating/backups">backups</a>, <a href="/guides/operating/rotate-postgres-password">password rotation</a>) reaches it through the container.</p><p>Since CLI 3.20.0, the template (version 6.4) publishes neither port. Update the CLI, then the installation:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --upgrade</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> arcsecond</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">arcsecond</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span></span></code></pre></div><p><code>arcsecond update</code> rewrites <code>docker-compose.yml</code> in place when it has not been edited by hand. If it has, the file is left alone and the packaged version lands beside it as <code>docker-compose.latest.yml</code>: remove the <code>ports:</code> block of the <code>db</code> and <code>broker</code> services from your file (or merge the two files by hand), then run <code>arcsecond start</code>.</p><p>There is no need to stop or remove the other PostgreSQL. If you would rather know what holds the port anyway: <code>netstat -ano | findstr :5432</code> in PowerShell prints the process id in the last column, and <code>tasklist /FI &quot;PID eq &lt;pid&gt;&quot;</code> names it. On macOS and Linux, <code>lsof -i :5432</code> does both.</p><h2 id="linux-permission-denied-while-trying-to-connect-to-the-docker-api" tabindex="-1">Linux: &quot;permission denied while trying to connect to the docker API&quot; <a class="header-anchor" href="#linux-permission-denied-while-trying-to-connect-to-the-docker-api" aria-label="Permalink to “Linux: &quot;permission denied while trying to connect to the docker API&quot;”">​</a></h2><p><code>arcsecond start</code> says that this user is not allowed to talk to Docker; the underlying line is <code>permission denied while trying to connect to the docker API at unix:///var/run/docker.sock</code>, on a Linux host running Docker Engine (not Docker Desktop). The daemon only answers root and members of the <code>docker</code> group, and a fresh install does not put your user there. <code>docker login</code> succeeds regardless, because it only writes a config file, which is why the failure looks sudden.</p><p>Add your user to the group once, then log out and back in (or <code>newgrp docker</code> in the current shell), and check with <code>arcsecond status</code> before running <code>arcsecond start</code> again:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> usermod</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -aG</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $USER</span></span></code></pre></div><h2 id="the-xyz-variable-is-not-set-defaulting-to-a-blank-string" tabindex="-1">&quot;The &#39;xyz&#39; variable is not set. Defaulting to a blank string.&quot; <a class="header-anchor" href="#the-xyz-variable-is-not-set-defaulting-to-a-blank-string" aria-label="Permalink to “&quot;The &#39;xyz&#39; variable is not set. Defaulting to a blank string.&quot;”">​</a></h2><p>Repeated <code>WARN[0000] The &quot;...&quot; variable is not set</code> lines in the progress <code>arcsecond start</code> shows, with names that look like random fragments, come from a <code>$</code> inside one of the secret keys that <code>arcsecond setup</code> wrote to <code>.env</code>. Compose interpolates <code>$name</code> in that file, so the <code>$</code> and the characters after it are dropped. Versions of the CLI up to 3.17.1 could generate such keys; later versions do not.</p><p>The containers all see the same shortened value, so an installation that runs with these warnings is fine as it is. On a <strong>brand new</strong> installation that has not started yet, the cleaner move is to open <code>.env</code> and remove every <code>$</code> from <code>SECRET_KEY</code>, <code>AUTH_JWT_SIGNING_KEY</code> and <code>AGENT_JWT_SIGNING_KEY</code> (or delete <code>.env</code> and rerun <code>arcsecond setup</code> with an up-to-date CLI). Do not touch these three keys on an installation that already has users: they sign every session token, and changing them logs everyone out.</p><h2 id="could-not-find-an-arcsecond-local-installation" tabindex="-1">&quot;Could not find an Arcsecond.local installation&quot; <a class="header-anchor" href="#could-not-find-an-arcsecond-local-installation" aria-label="Permalink to “&quot;Could not find an Arcsecond.local installation&quot;”">​</a></h2><p>Every <code>arcsecond</code> command that operates the installation (<code>start</code>, <code>stop</code>, <code>status</code>, <code>logs</code>, <code>update</code>, <code>restart</code>, <code>backups</code>, <code>db</code>) looks for it in the current folder, then in the folder <code>arcsecond setup</code> last ran in. The message lists both places it looked. Either open a terminal in the Arcsecond folder, or name it: <code>arcsecond start --dir C:\\Users\\&lt;name&gt;\\Arcsecond</code>. If <code>setup</code> has never run on this machine — say, the installation was copied over from another one — run <code>arcsecond setup</code> once in that folder: it changes nothing in existing files and records the location.</p><h2 id="the-container-name-is-already-in-use" tabindex="-1">&quot;The container name … is already in use&quot; <a class="header-anchor" href="#the-container-name-is-already-in-use" aria-label="Permalink to “&quot;The container name … is already in use&quot;”">​</a></h2><p>Shown by <code>arcsecond start</code> when a container named <code>arcsecond-db</code>, <code>arcsecond-api</code>… already exists on the machine but belongs to another installation: an older folder you set up once and forgot, or a development checkout. Docker allows one container per name. Either start <em>that</em> installation from its own folder, or remove the leftover container with the name Docker printed: <code>docker rm -f arcsecond-db</code>. Removing a container never touches the database volume.</p>`,21)])])}const k=o(s,[["render",i]]);export{u as __pageData,k as default};
